export class Converter {
  textToPirate(originalText: string) {
    let convertedText = "";
    for (let i = 0; i < originalText.length; i++) {
      convertedText += this.blabla(originalText[i]);
    }

    return convertedText;
  }

  blabla(currentLetter: string) {
    if (currentLetter === "b" || currentLetter === "B") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "c" || currentLetter === "C") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "d" || currentLetter === "D") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "f" || currentLetter === "F") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "g" || currentLetter === "G") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "h" || currentLetter === "H") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "j" || currentLetter === "J") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "k" || currentLetter === "K") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "l" || currentLetter === "L") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "m" || currentLetter === "M") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "n" || currentLetter === "N") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "p" || currentLetter === "P") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "q" || currentLetter === "Q") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "r" || currentLetter === "R") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "s" || currentLetter === "S") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "t" || currentLetter === "T") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "v" || currentLetter === "V") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "w" || currentLetter === "W") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "x" || currentLetter === "X") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else if (currentLetter === "z" || currentLetter === "Z") {
      const temp = currentLetter;
      currentLetter += "o";
      currentLetter += temp;
      return currentLetter;
    } else {
      return currentLetter;
    }
  }
}
